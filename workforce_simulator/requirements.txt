# Workforce Simulator dependencies.
# Kept minimal. pandas powers CSV loading/output; the rest serve the API.
pandas>=1.3

# API layer
fastapi
uvicorn
pydantic
python-multipart

# Brief-upload feature (optional): deterministic text extraction from uploaded
# .docx / text-based PDF briefs, plus the Anthropic SDK used only to draft
# editable tasks from that text. The AI-drafting endpoint degrades gracefully
# (HTTP 503) when ANTHROPIC_API_KEY is not set, so these stay import-guarded.
python-docx
pypdf
anthropic

# Employee Digital Twin Seed upload: read .xlsx seed files (pandas read_excel
# needs openpyxl). .csv uses pandas directly.
openpyxl
